# OTTO 审核助手插件开发规划

## 项目概述
- **插件名称**: `astrbot_plugin_otto_audit`
- **功能**: 用户通过 AI 发出审核指令 → 插件调用 OTTO Hub API 获取待审内容 → 调用多模态 AI 审核 → 自动执行通过/驳回操作 → 结果反馈给 AI → AI 回复用户
- **API 端点**: `api.ottohub.cn`
- **协议**: HTTP/HTTPS
- **数据格式**: JSON
- **字符编码**: UTF-8

---

## 工作流程

```
用户 → AI（LLM）→ 插件（LLM Tool） → OTTO Hub API（获取内容） → 多模态 LLM（审核） → 插件（自动通过或标记人工） → AI → 用户
```

1. **用户发出指令**: "帮我审核一下视频 ID 为 123 的内容"
2. **AI 调用插件**: LLM Tool → `audit_video(vid=123)`
3. **插件获取内容**: 调用 OTTO Hub API 获取待审列表或详情
4. **多模态 AI 审核**: 将内容（文本/图片/视频）传给多模态 LLM，返回标准 JSON
5. **执行操作**:
   - 内容合规 → 自动调用 approve 通过
   - 存在违规 → **不驳回**，返回提示"AI认为存在XXX内容，请耐心等待人工复核"
6. **结果返回**: 插件将结果传回给 AI，AI 回复用户

---

## 认证方式

**没有 API Key，通过 login 获取 token**

### 登录获取 Token
- **接口**: `POST /api/auth/login`
- **参数**: `uid_email`（UID 或邮箱）、`pw`（密码）
- **成功返回**:
  ```json
  {
    "uid": 123,
    "token": "xxxxxx",
    "avatar_url": "...",
    "cover_url": "...",
    "if_today_first_login": true,
    "email": "...",
    "is_audit": true,
    "is_admin": false
  }
  ```
- **错误**: `missing_argument` / `error_password`
- **说明**: 返回的 `is_audit` 判断是否是审核员；`token` 为后续所有请求的凭证

### Token 传递方式
- **GET 请求**: Query 参数，如 `?token=xxx`
- **POST/PUT/DELETE 请求**: JSON Body，如 `{"token": "xxx"}`

---

## OTTO Hub Moderation API 对接

### 获取待审列表（审核员权限）

| 内容类型 | 请求方式 | 端点 | 路径参数 | ID 标识 | 查找方式 |
|---------|---------|------|---------|--------|---------|
| 视频 | `GET` | `/api/moderation/videos` | `?token=&offset=&num=` | `vid` | 通过 vid |
| 动态 | `GET` | `/api/moderation/blogs` | `?token=&offset=&num=` | `bid` | 通过 bid |
| 头像 | `GET` | `/api/moderation/avatars` | `?token=&offset=&num=` | `uid` | 通过用户 UID |
| 封面 | `GET` | `/api/moderation/covers` | `?token=&offset=&num=` | `uid` | 通过用户 UID |

**通用参数**: `offset`（偏移量，默认0）、`num`（限制数量，默认20，最大100）

### 审核操作

#### 通过审核（approve）
```
PUT /api/moderation/{type}/{id}/approve
Body: {"token": "xxx"}
```

**内容类型映射**:
| 类型 | 端点 | ID 字段 |
|------|------|--------|
| 视频 | `/api/moderation/videos/{vid}/approve` | `vid`（视频 ID） |
| 动态 | `/api/moderation/blogs/{bid}/approve` | `bid`（动态 ID） |
| 头像 | `/api/moderation/avatars/{uid}/approve` | `uid`（用户 UID） |
| 封面 | `/api/moderation/covers/{uid}/approve` | `uid`（用户 UID） |

#### 驳回审核（reject）
```
PUT /api/moderation/{type}/{id}/reject
Body: {"token": "xxx", "reason": "驳回理由"}
```
- **所有驳回操作强制要求提供 `reason` 参数**
- `reason` 字数不超过 **50 字**

#### 举报操作（report）
```
POST /api/moderation/{type}/{id}/report
Body: {"token": "xxx", "reason": "举报理由"}
```
- 所有已登录用户可举报

#### 申诉操作（appeal）
```
POST /api/moderation/{type}/{id}/appeal
Body: {"token": "xxx", "reason": "申诉理由"}
```
- 只能由内容作者本人执行
- 只能对已被驳回的内容进行申诉

### 审核状态说明
- **视频/动态**: 0=待审核, 1=已通过, 2=已驳回
- **头像/封面**: 0=待审核, 正整数=已通过, -1=已驳回

### 响应格式
- **成功**: `{"status": "success", "data": {...}}`
- **错误**: `{"status": "error", "message": "error_code"}`

### 回避机制
- 不能审核自己上传的内容 → `cannot_review_own_content`
- 不能审核自己举报的内容 → `cannot_review_own_report`

---

## 多模态 AI 审核

### 审核内容获取
从 OTTO Hub API 获取待审内容后，需要将内容传递给多模态 LLM：
- **视频**: `video_url`（视频文件）+ `title` + `intro` + `cover_url`（封面）
- **动态**: `content`（文本）
- **头像**: `avatar_url`（图片 URL）
- **封面**: `cover_url`（图片 URL）

### 审核策略
- **内容合规** → 自动调用 `approve` 接口通过审核
- **存在违规** → **不驳回**，返回提示信息交由人工复核

### LLM 审核响应格式（标准 JSON）
```json
{
  "passed": true,
  "reason": "内容合规，无违规内容"
}
```
```json
{
  "passed": false,
  "reason": "包含不文明用语"
}
```

---

## 核心代码结构

```
test/
├── __init__.py                  # 插件入口，声明 Star 类
├── main.py                      # 主逻辑，注册 LLM Tool 和命令
├── models.py                    # 数据模型
├── _conf_schema.json            # 配置页面 schema
├── core/
│   ├── auth.py                  # 登录认证，获取 token 和 token 管理
│   ├── api_client.py            # OTTO Hub Moderation API 客户端
│   ├── auditor.py               # 审核逻辑（对接多模态 LLM）
│   └── parser.py                # 命令/参数解析
└── utils.py                     # 工具函数
```

## 配置项设计（_conf_schema.json）

| 配置项 | 类型 | 默认值 | 说明 |
|-------|------|--------|------|
| `otto_base_url` | string | `https://api.ottohub.cn` | OTTO Hub API 基础地址 |
| `otto_uid_email` | string | "" | 登录 UID 或邮箱 |
| `otto_password` | password | "" | 登录密码 |
| `audit_llm_provider` | string | "" | 审核用的 LLM Provider 节点 ID |
| `audit_llm_model` | string | "" | 审核用的多模态模型名 |
| `auto_execute` | bool | false | 是否自动执行审核操作（通过/驳回） |

---

## 开发阶段

### 第一阶段：基础搭建
1. 创建目录结构
2. 实现 `auth.py` — 登录获取 token、token 缓存与刷新
3. 实现 `api_client.py` — 封装所有 moderation API 调用
4. 实现 `main.py` — 注册 LLM Tool 和命令入口
5. 创建 `_conf_schema.json` — 配置页面
6. 创建 `models.py` — 数据模型

### 第二阶段：核心审核逻辑
1. 实现 `auditor.py` — 对接多模态 LLM 进行内容审核
2. 实现审核响应解析
3. 实现自动执行通过/驳回
4. 命令接入：`/审核列表` 获取待审列表

### 第三阶段：交互与 LLM Tool
1. 实现 LLM Tool `audit_content` — AI 可直接调用的审核工具
2. 实现按 ID 查询指定内容的功能
3. 命令接入：`/审核` 按 ID/类型审核
4. 结果格式化与回传

### 第四阶段：完善与测试
1. 错误处理完善（token 过期重登录、API 错误重试）
2. 日志完善
3. 用户权限检查（is_audit 判断）
4. 测试

---

## LLM Tool 设计

### audit_content（通用审核工具）
```python
@llm_tool(name="audit_content")
async def tool_audit_content(
    self,
    event: AstrMessageEvent,
    content_type: str,   # video/blog/avatar/cover
    content_id: int,     # 对应类型的 ID
) -> str:
    """
    审核 OTTO Hub 上的内容。调用后自动获取内容、调用多模态 AI 审核。
    合规内容直接通过，存在违规则标记并提示人工复核，不会自动驳回。
    Args:
        content_type: 内容类型, 可选: video/blog/avatar/cover
        content_id: 内容 ID
          - video: 通过 vid 查找
          - blog: 通过 bid 查找
          - avatar: 通过用户 uid 查找
          - cover: 通过用户 uid 查找
    """
```

### audit_list（获取待审列表工具）
```python
@llm_tool(name="audit_list")
async def tool_audit_list(
    self,
    event: AstrMessageEvent,
    content_type: str = "",  # 可选，筛选类型
    num: int = 10,           # 数量
) -> str:
    """
    获取 OTTO Hub 上的待审核内容列表。
    Args:
        content_type: 内容类型（可选）, 为空则返回全部类型的待审数量概览
        num: 需要获取的数量
    """
```
